# Реплики ментора: эпизод «Кредиты для бизнеса» (день 7)

Вступление звучит один раз, до туториала. Реплики об ошибках — при каждой ошибке, под ними ментор
показывает подсказку по заявке. Похвала за верное решение — только за первое верное решение каждого
вида заявки. Положите в эту папку двадцать файлов:

- `mr13-1.wav` — что такое кредит и ключевая ставка 14% (строка 291);
- `mr13-2.wav` — четыре вида заявок и что с ними делать (строка 292);
- `mr13-3.wav` — ошибка: деньги провальной идее (строка 293);
- `mr13-4.wav` — ошибка: отказ спорной идее (строка 294);
- `mr13-5.wav` — ошибка: вся сумма спорной идее (строка 295);
- `mr13-6.wav` — ошибка: транш кончается раньше проверки идеи (строка 296);
- `mr13-7.wav` — ошибка: транш оплачивает рост до проверки идеи (строка 297);
- `mr13-8.wav` — ошибка: отказ надёжному клиенту (строка 298);
- `mr13-9.wav` — ошибка: транш надёжному клиенту (строка 299);
- `mr13-10.wav` — ошибка: ставка не выше ключевой (строка 300);
- `mr13-11.wav` — ошибка: ставка корпорации выше 18% (строка 301);
- `mr13-12.wav` — ошибка: отсрочка корпорации (строка 302);
- `mr13-13.wav` — ошибка: ставка малого бизнеса выше 20% (строка 303);
- `mr13-14.wav` — ошибка: малому бизнесу не дали отсрочку (строка 304);
- `mr13-15.wav` — верно: отказ провальной идее (строка 305);
- `mr13-16.wav` — верно: первый транш спорной идее (строка 306);
- `mr13-17.wav` — верно: корпорация (строка 307);
- `mr13-18.wav` — верно: малый бизнес (строка 308);
- `mr13-19.wav` — все заявки без ошибок (строка 309);
- `mr13-20.wav` — все заявки, ошибки были (строка 310).
